<!DOCTYPE html>
<html>
  <head>
    <title>Loren Glenn</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
      <nav class = "nav navbar navbar-inverse shift">
        <%= link_to "Loren Glenn", root_path %> |
        <%= link_to "Projects", projects_path %> |
        <%= link_to "About", about_index_path %>
          <div class="pull-right shift">
          <% if current_admin %>
            Logged in as Admin</li>
             <span class="glyphicon glyphicon-align-justify" aria-hidden="true"></span> <%= link_to "Log-out", destroy_admin_session_path, method: "get" %>
          <% else %>
             <span class="glyphicon glyphicon-off" aria-hidden="true"></span>
            <%= link_to "Log in as Admin", new_admin_session_path %></a> |
             <%= link_to "New Admin", new_admin_registration_path %></a>
          <% end %>
        </div>
      </nav>
        <div class="container">
          <div class="alert-warning">
          <%= flash[:alert] %>
          <%= flash[:notice] %>
        </div>
          <%= yield %>
      </div>
  </body>
</html>
